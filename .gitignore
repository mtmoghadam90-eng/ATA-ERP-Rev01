node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
uploads/

# The former document store. Business data lives in SQL Server now, but a
# server upgraded from an older build still has this file on disk.
database.json
database.json.tmp
database.sqlite

# Signs the session cookies. Per-deployment, and committing it would let anyone
# forge a session.
.session-secret
.session-secret.tmp

# Machine-specific Claude Code permission grants (launch.json IS shared)
.claude/settings.local.json

# Data backups produced by scripts/deploy.ps1 on the server
backups/


# Prisma generated client
generated/
prisma/generated/

# Server logs (see src/server/logFile.ts) — one file per day, local to each host.
logs/

# The WhatsApp linked-device credentials (src/server/services/messaging/whatsappClient.ts).
# Whoever holds these files can send and read as the company's own line, so they
# are per-host and never committed — re-link by scanning the code again.
whatsapp-session/
